{
  "port": {{.ThisIsFinePort}},
  "services": [
  {{- $first := true }}
  {{- range $i, $p := .Projects }}
    {{- if $p.isDockerProject }}{{if not $first}},{{end}}
    "{{$.OrgName}}/{{$p.name}}"
    {{- $first = false }}{{end}}
  {{- end }}
  ]
}
